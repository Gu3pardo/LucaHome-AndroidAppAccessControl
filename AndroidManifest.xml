<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="guepardoapps.lucahome.accesscontrol"
    android:versionCode="402"
    android:versionName="0.4.2.170218" >

    <uses-sdk
        android:minSdkVersion="23"
        android:targetSdkVersion="25" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:configChanges="orientation|keyboardHidden"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
        <activity android:name="guepardoapps.lucahome.accesscontrol.view.Main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="guepardoapps.lucahome.accesscontrol.services.ControlServiceStateService"
            android:enabled="true" />
        <service
            android:name="guepardoapps.lucahome.accesscontrol.services.MainService"
            android:enabled="true" />
        <service
            android:name="guepardoapps.lucahome.accesscontrol.services.ReceiverService"
            android:enabled="true" />
        <service
            android:name="guepardoapps.lucahome.accesscontrol.services.RESTService"
            android:enabled="true" />
    </application>

</manifest>